<%= bulma_form_with model: [taikai, participating_dojo, team] do |f| %>
  <% if team.errors.any? %>
    <h2><%= pluralize(team.errors.count, "error") %> prohibited this team from being saved:</h2>
  <% end %>

  <%= f.input :index, input_html: {placeholder: "Automatically set if blank" }%>

  <%= f.button "Save", class: "button has-background-light mr-3" %>
  <%= link_to "Cancel", edit_taikai_participating_dojo_path(taikai, participating_dojo), class: "button" %>
<% end %>