<% Result::ENTEKI_VALUES.each do |value| %>
  <%= button_to update_marking_path(taikai, participant,  value: value, match_id: match&.id),
                method: :post,
                class: "button is-small ml-1",
                title: t('.mark_hit') do %>
    <%= value %>
  <% end %>
<% end %>
