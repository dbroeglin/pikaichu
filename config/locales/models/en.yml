en:
  activerecord:
    models:
      taikai: Taikai
    enums:
    attributes:
      dojo:
        shortname: Code
        name: Name
        country_code: Country
      taikai:
        shortname: Code
    errors:
      models:
        match:
          attributes:
            winner:
              defined_results_for_target: cannot be declared when target match already has results
        participant:
          attributes:
            index_in_team:
              blank: must be defined if participant is a team member
              taken: must be unique within a team
        team:
          attributes:
            index:
              taken: must be unique within a participating dojo
