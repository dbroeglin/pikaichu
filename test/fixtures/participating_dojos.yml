
participating_dojo2_individual_dist_12:
  taikai: individual_dist_12
  dojo: dojo_hk
  display_name: HK

hk_2in112:
  taikai: 2in1_dist_12
  dojo: dojo_hk
  display_name: HK

participating_dojo1_taikai5:
  taikai: other_taikai
  dojo: dojo_fr
  display_name: FR

AK_2in1_test:
  taikai: 2in1_test
  dojo: dojo_fr
  display_name: AK

<%
  [true, false].each do |distributed|
    [12, 20].each do |total_num_arrows|
      %w(individual team 2in1).each do |form|
        [true, false].each do |enteki|
          label = "participating_dojo1_#{form}#{distributed ? '_dist' : ''}_#{total_num_arrows}#{enteki ? '_enteki' : ''}"
          #puts label + " #{Zlib.crc32(label) % (2**30 - 1)}"
%>
<%= label %>:
  taikai: <%= form %><%= distributed ? '_dist' : '' %>_<%= total_num_arrows %><%= enteki ? '_enteki' : '' %>
  dojo: dojo_fr
  display_name: <%= label.titleize %>
<%
        end
      end
    end
  end
%>

<%
  [true, false].each do |distributed|
    [4].each do |total_num_arrows|
      %w(matches).each do |form|
        [true, false].each do |enteki|
          label = "participating_dojo1_#{form}#{distributed ? '_dist' : ''}_#{total_num_arrows}#{enteki ? '_enteki' : ''}"
          #puts label + " #{Zlib.crc32(label) % (2**30 - 1)}"
%>
<%= label %>:
  taikai: <%= form %><%= distributed ? '_dist' : '' %>_<%= total_num_arrows %><%= enteki ? '_enteki' : '' %>
  dojo: dojo_fr
  display_name: <%= label.titleize %>
<%
        end
      end
    end
  end
%>