<%
  [true, false].each do |distributed|
    [12, 20].each do |total_num_arrows|
%>
individual<%= distributed ? '_dist_' : '' %><%= total_num_arrows %>:
  shortname: individual-<%= distributed ? 'dist-' : '' %><%= total_num_arrows %>
  name: Individual <%= distributed ? 'Distributed ' : '' %><%= total_num_arrows %>
  start_date: <%= 5.days.from_now %>
  end_date: <%= 6.days.from_now %>
  num_targets: 6
  total_num_arrows: <%= total_num_arrows %>
  tachi_size: 3
  distributed: <%= distributed %>
  form: individual

team<%= distributed ? '_dist_' : '' %><%= total_num_arrows %>:
  shortname: team-<%= distributed ? 'dist-' : '' %><%= total_num_arrows %>
  name: Team <%= distributed ? 'Distributed ' : '' %><%= total_num_arrows %>
  start_date: <%= 10.days.from_now %>
  end_date: <%= 12.days.from_now %>
  num_targets: 6
  total_num_arrows: <%= total_num_arrows %>
  tachi_size: 3
  distributed: <%= distributed %>
  form: team

2in1<%= distributed ? '_dist_' : '' %><%= total_num_arrows %>:
  shortname: 2in1-<%= distributed ? 'dist-' : '' %><%= total_num_arrows %>
  name: "2 in 1 <%= distributed ? 'Distributed ' : '' %><%= total_num_arrows %>"
  start_date: <%= 16.days.from_now %>
  end_date: <%= 18.days.from_now %>
  num_targets: 6
  total_num_arrows: <%= total_num_arrows %>
  tachi_size: 3
  distributed: <%= distributed %>
  form: 2in1

matches<%= distributed ? '_dist_' : '' %><%= total_num_arrows %>:
  shortname: matches-<%= distributed ? 'dist-' : '' %><%= total_num_arrows %>
  name: "Matches <%= distributed ? 'Distributed ' : '' %><%= total_num_arrows %>"
  start_date: <%= 16.days.from_now %>
  end_date: <%= 18.days.from_now %>
  num_targets: 6
  total_num_arrows: <%= total_num_arrows %>
  tachi_size: 3
  distributed: <%= distributed %>
  form: matches

<%
    end
  end
%>

other_taikai: # Admin is Alain Térieur
  shortname: other-taikai
  name: Taikai 5
  start_date: <%= 20.days.from_now %>
  end_date: <%= 24.days.from_now %>
  num_targets: 6
  total_num_arrows: 12
  tachi_size: 3
  distributed: true
  form: team

2in1_test:
  shortname: 2in1-test
  name: "2 in 1 / test"
  start_date: <%= 16.days.from_now %>
  end_date: <%= 18.days.from_now %>
  num_targets: 6
  total_num_arrows: 12
  tachi_size: 3
  distributed: false
  form: 2in1
